apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: lore-ui
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: lore-ui
